#!/bin/sh
#
# Configure options script for re-calling MagickCore compilation options
# required to use the MagickCore library.
#

prefix=/opt/local/www/apache2/html/imagick/image_magick
exec_prefix=/opt/local/www/apache2/html/imagick/image_magick

usage="\
Usage: MagickCore-config [--cflags] [--cppflags] [--exec-prefix] [--ldflags] [--libs] [--prefix] [--version]"

if test $# -eq 0; then
      echo "${usage}" 1>&2
      echo "Example: gcc \`MagickCore-config --cflags --cppflags\` -o core core.c \`Magick-config --ldflags --libs\`" 1>&2
      exit 1
fi

while test $# -gt 0; do
  case "$1" in
    -*=*) optarg=`echo "$1" | sed 's/[-_a-zA-Z0-9]*=//'` ;;
    *) optarg= ;;
  esac
  case $1 in
    --prefix=*)
      prefix=$optarg
      ;;
    --prefix)
      echo $prefix
      ;;
    --exec-prefix=*)
      exec_prefix=$optarg
      ;;
    --exec-prefix)
      echo $exec_prefix
      ;;
    --version)
      echo '6.9.12-62 Q8 '
      ;;
    --cflags)
      /usr/local/bin/pkg-config --cflags MagickCore-6.Q8
      ;;
    --cxxflags)
      /usr/local/bin/pkg-config --cflags MagickCore-6.Q8
      ;;
    --cppflags)
      /usr/local/bin/pkg-config --cflags MagickCore-6.Q8
      ;;
    --ldflags)
      /usr/local/bin/pkg-config --libs MagickCore-6.Q8
      ;;
    --libs)
      /usr/local/bin/pkg-config --libs MagickCore-6.Q8
      ;;
    *)
      echo "${usage}" 1>&2
      exit 1
      ;;
  esac
  shift
done
